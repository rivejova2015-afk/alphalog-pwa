// Auto-Summaries IA
// Resumen de 2–3 líneas por evidencia (lectura rápida) sin alterar el contenido original.

export function autoSummarizeEvidence(content: string): string {
  // Simulación: toma las 2 primeras líneas
  return content.split("\n").slice(0, 2).join(" ");
}
